import {Channel} from "./Channel";

/**
 * 断言
 *
 * @author noear
 * @since 2.0
 */
export class Asserts {
    /**
     * 断言关闭
     */
    static assertClosed(channel: Channel): void {

    }

    /**
     * 断言关闭
     */
    static assertClosedByUser(channel: Channel): void {

    }
}